# Copy this file to .env and fill in the values for local development.
# Never commit .env — it is gitignored.

# ── Stellar Network ───────────────────────────────────────────────────────────

# Which network to connect to. Determines the network passphrase automatically.
# Options: testnet (default), mainnet
NETWORK=testnet

# Horizon REST API — used to watch for inbound payments via SSE.
# Defaults to the public testnet endpoint.
HORIZON_URL=https://horizon-testnet.stellar.org

# Stellar RPC — used to simulate and submit Soroban transactions.
# Defaults to the public testnet endpoint.
RPC_URL=https://soroban-testnet.stellar.org

# ── Pool Accounts ─────────────────────────────────────────────────────────────

# The relayer watches one Horizon SSE stream per pool account.
# Add more accounts by incrementing the index (POOL_ADDRESS_2, POOL_PRIVATE_KEY_2, etc.)
# The address and private key must match — the relayer validates this at startup.
# Required: at least POOL_ADDRESS_1 + POOL_PRIVATE_KEY_1.
POOL_ADDRESS_1=G...
POOL_PRIVATE_KEY_1=S...

# ── Channel accounts (optional) ───────────────────────────────────────────────

# Stellar accepts one pending transaction per source account, so a pool alone
# lands at most one forward per ledger (~12/min). With channels, each forward
# or sweep uses a leased channel account as its transaction source: the pool
# still authorizes the payment and pays the fee (fee-bump), and forwards run
# in parallel, one per channel. Off when unset or 0.
# Create the accounts with `make deposit-channels` (funded by pool 1; each
# needs only its base reserve). The seed must differ from the gasless
# service's CHANNEL_SEED. Generate with: openssl rand -hex 32
# DEPOSIT_CHANNEL_COUNT=10
# DEPOSIT_CHANNEL_SEED=

# Names this instance's channel leases, for debugging. Defaults to hostname-pid.
# INSTANCE_ID=

# ── Recovery ──────────────────────────────────────────────────────────────────

# G-address that receives deposits with no matching intent (missing or unknown memo).
# Use a separate account you control — not a pool address.
# Required.
RECOVERY_ADDRESS=G...

# ── Accepted assets ───────────────────────────────────────────────────────────

# Assets deposits may be credited in: comma-separated "native" and CODE:ISSUER.
# Anything else is swept to RECOVERY_ADDRESS. Defaults to XLM plus Circle's
# USDC for NETWORK. The pools and the recovery account need an authorized
# trustline for each issued asset; the relayer logs any missing one at startup.
# ACCEPTED_ASSETS=native,USDC:GBBD47IF6LWK7P7MDEVSCWR7DPUWV3NY3DTQEVFL4NAT4AQH3ZLLFLA5

# ── Database ──────────────────────────────────────────────────────────────────

# PostgreSQL connection string (pgx/v5 format).
# Required.
DATABASE_URL=postgres://user:password@localhost:5432/latch_relayer?sslmode=disable

# ── HTTP Server ───────────────────────────────────────────────────────────────

# Port the relayer HTTP server listens on. Defaults to 4000.
PORT=4000

# Shared secret every caller must send as `Authorization: Bearer <key>`.
# Only /health is exempt. latch-api is the intended caller — this is a
# service-to-service secret, not a user token.
# Required, minimum 32 characters. Generate with: openssl rand -hex 32
RELAYER_API_KEY=

# ── Capacity (all optional; defaults shown) ───────────────────────────────────

# Postgres pool size. pgx's own default is max(4, CPUs) — too small under load.
# On Neon, point DATABASE_URL at the -pooler endpoint and keep
# DB_MAX_CONNS × instances under the plan's connection limit.
# DB_MAX_CONNS=20
# DB_MIN_CONNS=2

# Concurrent requests before new ones get 503 + Retry-After (load shedding).
# MAX_INFLIGHT_REQUESTS=500

# Per-caller token bucket (after auth): sustained requests/second and burst.
# RATE_LIMIT_RPS=100
# RATE_LIMIT_BURST=300

# Per-request timeout for Stellar RPC calls (simulate/send/poll).
# RPC_TIMEOUT_MS=15000

# On SIGTERM: stop taking work, then give in-flight requests and forwards this
# long to finish. Keep it below the host's shutdown grace period.
# SHUTDOWN_DRAIN_SECONDS=30

# How often the retry worker sweeps pending_retry forwards (seconds).
# RETRY_INTERVAL_SEC=10

# For running DB-backed tests locally (skipped when unset):
# TEST_DATABASE_URL=postgres://user@localhost:5432/latch_relayer_test?sslmode=disable
